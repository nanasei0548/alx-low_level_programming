#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point
 * Return: Always 0 (Success)
 */

int main(void)

{
	int d;

	for (d = 'a'; d <= 'z'; d++)

		putchar(d);

	putchar('\n');

	return (0);
}
