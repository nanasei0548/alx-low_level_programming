#include<stdio.h>
#include<stdlib>
#include<time.h>
/**
 * main - Entry Point
 * Return: Always 0 (Success)
 */

int main(void)
{
	int a:
	char b;

	for (a = '0'; a <= '9'; a++)
		putchar(a);
	for (b = 'x'; b <= 'y'; b++)
		putchar(b);
	putchar('\n');
	return (0);
}
